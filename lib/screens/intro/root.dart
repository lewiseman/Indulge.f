import 'package:flutter/material.dart';
import 'package:indulge_me/screens/intro/welcome/body.dart';

class IntroPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Welcome(),
    );
  }
}