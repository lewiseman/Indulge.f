import 'package:flutter/material.dart';

class AudioContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Audio content'),
      ),
    );
  }
}
